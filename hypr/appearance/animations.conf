$anim_time= 2.5
animations {
  enabled = true

  bezier = breezeIn, 0.08, 1.05, 0.12, 1.12
  bezier = breezeOut, 0.28, -0.25, 0.05, 1.02
  bezier = breezeMove, 0.06, 0.60, 0.1, 1.08
  bezier = breezeFade, 0.2, 0.8, 0.3, 1.0
  bezier = liner, 1, 1, 1, 1

  animation = windows, 1, $anim_time, breezeMove, slide
  animation = windowsIn, 1, $anim_time, breezeIn, slide
  animation = windowsOut, 1, $anim_time, breezeOut, slide
  animation = windowsMove, 1, $anim_time, breezeMove, slide
  animation = border, 1, $anim_time, liner
  animation = borderangle, 1, $anim_time, liner, once
  animation = fade, 1, $anim_time, breezeFade
  animation = workspaces, 1, $anim_time, breezeMove, slide
  animation = specialWorkspace, 0.3, $anim_time, breezeMove, slidevert
}
